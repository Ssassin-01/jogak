import 'package:flutter/material.dart';

class AppColors {
  // 배경색 (보랏빛이 감도는 깊은 수심)
  static const Color background = Color(0xFF070612);
  
  // 표면색 (반투명한 라벤더 네이비)
  static const Color surface = Color(0xFF1D1B36);
  static const Color surfaceLight = Color(0xFF2A284D);
  
  // 메인 색상 (잔잔한 물결의 실버)
  static const Color primary = Color(0xFFC0C0C0);
  static const Color primaryLight = Color(0xFFE0E0E0);
  
  // 포인트 색상 (수면 위 달빛의 샴페인 골드)
  static const Color secondary = Color(0xFFF1E4C3);
  static const Color secondaryLight = Color(0xFFFDFCF0);
  
  // 텍스트 색상 (어둠 속에서도 명확한 대비)
  static const Color textPrimary = Color(0xFFE0E0E0);
  static const Color textSecondary = Color(0xFF6A7B8B);
  static const Color textHint = Color(0xFF3F4E5A);
  
  // 경계선 색상 (거의 보이지 않는 안개 같은 느낌)
  static const Color divider = Color(0xFF1A1F2C);
  
  // 감정별 윤슬 조약돌 색상 (Reflective Orbs)
  static const Color moodJoy = Color(0xFFF1E4C3);     // 햇살 조약돌
  static const Color moodCalm = Color(0xFFC0C0C0);    // 은빛 조약돌
  static const Color moodSad = Color(0xFF30628A);     // 심해 조약돌
  static const Color moodExcited = Color(0xFFD4A5A5); // 노을 조약돌
  static const Color moodAngry = Color(0xFFA73B21);   // 불꽃 조약돌
}
